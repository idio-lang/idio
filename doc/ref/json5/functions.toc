(rst "functions-intro" 0)

(document json5 parse-file)
(document json5 parse-fd)
(document json5 parse-string)
(document json5 generate)
(document json5 generate-json)

;; no other values to document so here will do
(nodocument json5 version)
