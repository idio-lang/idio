#! /usr/bin/env bc

# FIB -- A classic benchmark, computes fib(30) inefficiently.

define fib (n) {
    if (n < 2) {
           return (n);
       } else {
	   return fib (n - 1) + fib (n - 2);
       }
}

fib (30)
